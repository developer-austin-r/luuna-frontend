services:
  frontend:
    image: ghcr.io/developer-austin-r/luuna-frontend:latest
    container_name: luuna-frontend

    env_file:
      - .env

    ports:
      - "3000:3000"

    restart: unless-stopped

    networks:
      - luuna-network

networks:
  luuna-network:
    external: true